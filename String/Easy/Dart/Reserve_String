class Solution {
  void reverseString(List<String> s) {
      int left = 0;
      int end = s.length-1;
      while (left < end) {
      //swap
        var temp = s[left];
        s[left] = s[end];
        s[end] = temp;
      
        left ++;
        end --;
      
    }
  }
}
