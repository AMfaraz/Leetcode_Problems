class Solution {
  bool rotateString(String s, String goal) {
    for(int i=0;i<s.length;i++){
        String first=s[0];
        s=s.substring(1,s.length)+first;
        if(s==goal){
            return true;
        }
    }
    return false;
  }
}
